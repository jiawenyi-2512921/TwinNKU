# Twin NKU 开发约定

适用于本仓库。用户本轮明确要求优先于旧方案和过期记录。

- 当前仓库是 `jiawenyi-2512921/TwinNKU`，不是带连字符的旧仓库。
- 2026-09-23用户明确授权GitHub作为代码、部署指导和素材传输渠道，图片可直接上传。大型图片资源包使用本仓库Releases交付，附SHA256和导入说明；当前完整楼层交付参见`docs/17-floor-release.md`；旧地图交接保留历史记录。
- 2026-09-23用户最新要求：只交付有标注地图和楼层图，不给未命名建筑设置点击；在综合业务西楼和东楼正中央添加周恩来雕像作为景点。此要求优先于本日早期的未命名区域方案和先前撤回雕像的记录。
- 2026-09-23后续反馈：恢复文字直接写在地图上的原有视觉；取消常驻浮动名称、白底标签与自动避让。保留原图烧录名称，周恩来雕像采用同图坐标、随地图缩放的普通文字补充，不恢复序号。83点点击与有标注楼层功能保持。
- 2026-09-24用户确认第20栋楼资料已完成，现纳入综合实验楼C区；只接收已标注原图，体育馆同层A/B/C分区保留为同一楼层的多张图。目录、原图接收、代码提交和服务器上线分别验收，不能相互替代。
- 本次仅升级楼层模块，不重新导入历史校园地图、不运行旧种子覆盖后台已发布点位。
- 当前分工：Codex负责实现、验证并同步代码与规范到GitHub；服务器部署由用户和DeepSeek执行。除非用户再次明确要求，后续不自动连接或部署服务器。交付时给出真实提交链接、验证结果和部署注意事项；写入被拒绝时准确报告，不把本地提交说成已上传。
- 先读 `docs/README.md` 和当前模块规范，按 `docs/06-module-plan.md` 增量实现。
- 技术基线：React/TypeScript/Vite、Leaflet CRS.Simple、FastAPI、PostgreSQL、NetworkX、NK-GeniOS适配器。
- 保留修订规划图的真实布局；不自行转向全校三维重建、实时定位或无依据的全校室内导航。
- 未验证的学校API、服务器状态和素材公开许可必须标为待核验。禁止硬编码假AI回复或用模拟路径冒充实际能力。
- 外部平台协议不得凭品牌猜测。平台密钥只进入后端环境，禁止提交或输出凭据。
- 公共DTO由Pydantic定义，运行接口和计划接口严格分开。修改后重新生成OpenAPI与前端类型。
- planned端点不得挂到生产router。模块完成后更新实现状态、能力开关和验证记录。
- 数据库只通过Alembic迁移；生产不使用SQLite，不自动清空/重建数据。
- 图片坐标是原图左上像素{x,y}；坐标转换只在地图适配层。路径必须关联图版本。
- 所有文件访问、模型动作、内容发布在后端执行权限和状态检查，前端隐藏不是授权。
- 按用户最新授权范围交付素材；运行资源包仍遵守模块清单及发布状态。外部原始全景和个人咨询等未包含在本次交付中的材料，依据实际来源和授权处理。
- 核心验证命令见README。不能把未运行的CI、容器、服务器或第三方验证记为通过。
- 说明改动后的行为、验证结果和具体剩余问题。基础能力正常时，新增模块应能独立关闭。

## v0.4.0 管理后台约束

- 当前增量分支`feat/admin-console`；部署方先读docs/15-admin-console.md。后台默认关闭，无默认密码；不要提交真实账号凭据。
- 四角色与校区/指定点位scope必须由后端强制执行；审核账号不得出现在该次变更的贡献者/提交者名单。
- 修改进入独立草稿，发布才修改公开版本；下架可恢复，无硬删除。保留revision冲突检查、操作记录和会话撤销。
- 地图原文件及图块不因后台更新重编码。`label_on_map`控制新增随图缩放文字，无法擦除图内烧录文字；雕像标注保持启用。
- 2026-09-24已增加楼层标注原图网页上传/分区维护与外部HTTPS VR链接编辑审核，参见docs/18-resource-editor.md；head为0005_resource_editor。未实现SSO、校园底图网页上传、楼层内部点位/路线和VR文件托管，不得描述为完整素材CMS。

## 2026-09-24 点位介绍增量

- 用户要求补齐所有点位介绍并规划精简前台。内容初稿位于data/introductions；只有经现有后台独立审核发布的记录才可对外展示/入AI知识库。
- scripts/stage_point_introductions.py仅调用现有认证后台接口，默认预览；不导入历史几何，不覆盖未完成草稿或默认替换非空介绍，不自动发布。部署见docs/19。
- docs/20是下一阶段UI、VR组织、AI联动与比赛目标计划，不能描述为已经实现；本轮仅改介绍排版与折叠来源。
