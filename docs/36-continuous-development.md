# 非智能体功能持续开发记录

用户于2026-09-26下午要求持续开发和优化其他功能，先暂停智能体接入。每小时安排一次迭代，直到用户要求停止；自动化创建成功不等于某次代码已经完成、提交或部署。每次运行以实际工具结果记录产出，权限失效需明确报告。

## 当前工作

2026-09-26 17:19（上海）：用户确认下一阶段功能目标，要求立即优先升级管理后台。第五批从`af4c41d`开始，修复统一审核入口遗漏楼层/VR、全局查找与工作台体验，记录见[37](37-admin-workbench.md)。现已完成统一队列、工作台真实统计、资料/审计搜索、读取恢复及前后对照；本地169项后端/84项前端通过，5项PostgreSQL留待CI。正交付完整代码和规范，GitHub最终状态需独立核对，后续迭代勿并发覆盖。第四批已交付，下面保留历史过程。

第四批（基线`a2ebcfa`）继续修复后台建筑列表分页、失败重试和读取竞态；最新完整提交的GitHub CI须独立核对。上传中或未提交修改属于本轮接续，后续自动化勿并发重写相同文件。第三批楼层原图恢复与只读验收报告已经交付，详情如下。

基线：`5d085aa`，分支`feat/admin-console`。第一批代码完成，前端52项回归和生产构建通过。GitHub按最后完整提交及对应CI交付，目录组中间提交不可用于部署：

- MAP-09：仅在浏览器保存地点ID的收藏、最近20个浏览地点、取消收藏和清空最近浏览；按校区隔离，过滤已下架地点，存储禁用时退为当前页面内存。
- MAP-04：中文空白分词、全角/大小写规范化，精确名称/别名优先；三个列表均支持分类和搜索。
- MAP-07 / RES-02：用户选点进入历史，后台同步仅替换失效地址；楼层浏览器后退/前进恢复和分区分享，直接打开分享链接后关闭大图不会退出本网站。

本批不修改API、数据库、地图锚点或图片，不新增依赖。构建、回归、GitHub与上线各自记录，不能互相替代。

第一批最终提交为`d49a7e8`，其[push CI](https://github.com/jiawenyi-2512921/TwinNKU/actions/runs/36230038251)与[PR CI](https://github.com/jiawenyi-2512921/TwinNKU/actions/runs/36230041409)均成功；137项后端测试（含真实PostgreSQL）、52项前端测试、契约/构建与compose-smoke通过。服务器和真机验收仍待执行。

第二批已实现公共JSON请求的15秒独立超时，覆盖等待响应与解析响应体；不依赖30秒轮询中止来恢复。原先楼层/全景的挂起请求会在轮询时被作为“取消”忽略，可能一直显示加载。现在超时在前端转为可重试错误，调用方的AbortSignal保持未取消，因此面板能进入现有重试状态。切换建筑/卸载导致的取消仍为AbortError；旧请求不会更新新点位。成功/失败均清理定时器和监听器。此机制只作用于本项目公共JSON API，不代理学校接口，也不用于全景网页或楼层图片字节传输。

第二批新增9项回归，完整前端61项与生产构建通过；其中3项执行实际公共API客户端代码，验证响应体挂起、超时与外部取消的区别、服务器错误状态和request_id保留。GitHub最终CI按该批完整提交独立核对，不沿用第一批绿勾。

第二批最终提交`c60ce15`的push CI `36230278400`与PR CI `36230280196`均成功，137项后端、61项前端及契约/构建/compose-smoke通过。这是第二批的证据，不能代替第三批结果。

第三批将Leaflet地图生命周期和图片尝试分开：失败/30秒超时提示重试，重试不重建地图；成功后释放计时器与监听器，卸载/切换后旧load/error不能更新当前状态。失败图层被移除，不保证浏览器立即取消其原生网络传输，不改变图片URL、尺寸或字节。新增7项有控制时钟的生命周期回归，完整前端68项与TypeScript/Vite构建通过。

只读资源验收沿用`scripts/verify_floor_release.py`，保留旧参数和`verify()`返回约定，新增`--json-out/--timeout/--max-seconds`。继续检查其他楼层并区分接口可读、字段一致和原图字节一致；报告记录清单摘要、检查时间、预期/通过数量及失败原因。更高审核revision不会触发自动导入/降级，额外公开楼层仅记录数量。新增32项回归，含真实本地HTTP、真实已发布API原图和撤回后不可读；完整测试与最终CI分别记录。用法与时间预算边界见[17](17-floor-release.md)。本轮没有增加产品API或后台功能，无数据库、依赖、地图几何、原图或平台SDK修改。

第三批本地完整后端165项通过、4项真实PostgreSQL因无本地实例跳过；Ruff通过。前端68项与构建通过。最终GitHub CI需补齐真实PostgreSQL和compose-smoke；本地测试不代表生产验收。

第三批最终提交`a2ebcfa`的push CI `36231363412`与PR CI `36231365340`均成功，包含真实PostgreSQL与compose-smoke。远程文件树已与本地测试代码逐项比对一致。

第四批核对发现资源后台的建筑查询固定`page_size=50`且没有page或翻页入口。现复用已有API分页，每页20项、搜索变化回第一页，并增加失败重试；仅切换候选列表不清空编辑区，真正选建筑沿用未保存确认。后台通用读取hook原先对成功响应没有取消检查，也会在path变空后留下loading；现补齐两处状态保护，不改变写入调用。新增3项执行实际hook源码effect/cleanup的竞态回归（不是React视觉测试），完整前端71项与TypeScript/Vite构建通过。无API、迁移、依赖、权限或已发布资料变动，只需重建web；第四批CI单独核对。

## 第一批使用与部署

地点目录新增“全部地点 / 我的收藏 / 最近浏览”三个范围，均可搜索和按类型筛选；每行和地点详情可收藏/取消。最近浏览去重保留20项，可直接清空；清空不会因当前详情重渲染而重新填回。收藏上限200，达到上限给出提示，不偷偷删除旧收藏。搜索使用Unicode NFKC规范化、大小写归一及空格分词，名称精确匹配优于别名精确匹配、前缀及普通包含；不添加未经核实的新别名。

浏览器保存内容仅为`version/favorites/recent`，列表里只有UUID；不保存地点名称/介绍、时间戳、搜索内容或账号信息，不上传服务器。页面用当前公开目录解析ID，已下架地点不展示；以后重新发布同一ID会重新出现在收藏中。不同校区使用不同存储键。普通跨标签页更新通过storage事件同步、每次修改前读取最新值；localStorage不是事务数据库，完全同时写入仍以最后写入为准。存储被拒绝/配额耗尽会显示“本次记录暂存于页面”，不影响地图和资料。

选点会增加浏览历史，同一点不重复增加；后台下架/链接纠正只替换当前历史项。打开楼层增加一个历史项，切换层/区在该项内更新，后退返回地点、前进恢复最后的层/区。关闭直接打开的分享链接只回到本地点，不会后退到来源网站。楼层图增加“复制本层链接”，保留渠道参数；剪贴板不可用时给出复制浏览器地址的提示。

部署方在已有`5d085aa`或后续兼容版本基础上，只需更新并重新构建web服务；无需迁移、重导校园图或楼层资料。具体Compose操作沿用[30前端部署](30-interface-release.md)。如同时补上更早未部署的SDK修复，仍按32说明核对API/web匹配，不能以本次纯前端范围跳过旧版本差异。回退只回退前端代码，浏览器ID记录可保留，旧前端会忽略它们。

## 验收记录

- 新增11项回归，完整前端52项通过；TypeScript与Vite生产构建通过。覆盖存储损坏/版本/超限、ID白名单、按校区隔离、最近排序和清空、收藏容量、不丢前一个标签页已完成修改、拒绝保存/配额降级、搜索排序和公开目录过滤，以及历史往返/直接深链接关闭/后台纠正。
- 这些是Node逻辑回归，不代表浏览器视觉或真机触控验证。当前环境的本地浏览器预览此前被策略阻止，未改用其他通道绕过。
- 服务器尚未部署；本批无后端或契约修改。最新GitHub CI须针对最终完整提交检查，不沿用基线绿勾。

部署后至少完成以下任务：

| 操作 | 预期 |
| --- | --- |
| 收藏两个地点，刷新页面，切换“我的收藏” | 当前公开地点仍在，可取消；不出现重复 |
| 连续打开A、B、A，查看“最近浏览”并清空 | A、B按最近顺序，清空后不立即回填；其他标签页同步 |
| 搜索全角字母/空格分词/别名，再切换收藏范围 | 匹配和顺序正确；空结果可以恢复全部 |
| A→B→浏览器后退/前进 | 地点详情和URL同步，校园底图不重建 |
| 打开楼层→换层/分区→复制链接→后退/前进 | 楼层返回和恢复正确，新窗口打开相同层/区 |
| 从外部直接打开楼层链接，再点返回地图 | 留在本网站、本建筑，URL清除楼层参数 |
| 手机窄屏与横屏、长建筑名、屏幕键盘 | 控件可见且可退出，无横向溢出；收藏/复制按钮可触达 |
| 地点或楼层下架、断网重试 | 公开撤回被同步；错误可重试，不能显示另一建筑旧资料 |
| 原图请求挂起30秒后恢复网络并重新加载 | 提示超时、重新加载成功，旧图片迟到不改变当前层/区 |
| 原图失败后重试、快速切层/关闭，期间旋转屏幕 | 重试不重建地图，离开后旧图不覆盖新选择；控件仍可操作 |
| 后台建筑列表超过50项，翻页后搜索/清空、断网重试 | 可访问后页，搜索回到首页，候选列表操作不丢编辑区；切换建筑仍检查未保存修改 |

人工任务结果仍待部署方记录，不写成已通过。

## 下一批候选，按证据选择

1. 公共JSON请求超时/取消已在第二批修复；原图加载与重试、只读验收报告已在第三批实现。不重复实现这些机制；后续只根据部署反馈修复，全景外链不能跨站判断其内部加载状态。
2. 第四批已补齐后台建筑分页与迟到读取保护。继续核对公开资源更新同步及后台读取超时：只对读取做恢复，不自动重试发布/审核等写入；用真实接口夹具证明撤回、改名、替换图片后列表和选择正确。
3. 部署方用第三批报告执行20栋96层100图的真实环境核验；人工语义与手机操作仍单独记录，不把测试夹具或语法兼容写成生产/旧版Python实测通过。
4. 在现有员工scope和审核流程下增加内容完整度/待办筛选；统计必须来自实际资料，不能用演示值填充。
5. 评估独立于智能体的线上参观进度。正式主题须先有审核站点与学习材料；不把点位顺序冒充步行路线或实测时长。

## 每次接续方式

1. 先读取最新用户要求、AGENTS、此记录和Git状态，再fetch远程。当前工作分支不是main；不得重置用户修改或强推。
2. 有未提交开发、正在提交的目录组或未完成CI时，先确认是否本轮接续；不得开启同文件的并发改写。无法确认则做独立只读核对并报告。
3. 选择一项有材料支撑的任务，记录“问题→改动→测试→提交→剩余限制”。已有功能优先修复，不随意替换技术栈。
4. 代码与规范提交GitHub开发分支，核对最后完整提交的CI。服务器部署继续由用户/DeepSeek执行。
5. 用户说停止时停止迭代并关闭自动化；智能体要等用户恢复指令，不能按日期自行假定已解除阻塞。
