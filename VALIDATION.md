# 2026-09-26 第五批：统一审核中心与管理工作台

- 基线af4c41d；本次合并三类草稿的只读查询，工作台统计、资料/审计搜索遵守原scope，独立审核和revision约束继续由原写接口执行。已有VR提交无需重建。
- 本地完整后端169项通过，5项PostgreSQL未配置实例跳过；Ruff和生成契约/参考/插件检查通过。新增4项HTTP回归和CI内1项PostgreSQL统一审核流程。
- 本地前端84项通过（新增7项后台和6项公开资源同步回归），TypeScript/Vite构建通过。用受控组件处理器/effect与API夹具，未执行浏览器视觉/真机或生产服务器验收。
- 同时接入此前本地abcb40a的公开资源同步改动，资源成功发布才通知，失败或仅保存不通知；相同VR响应保留对象身份。没有变动地图坐标、图片字节、依赖、数据库schema或智能体实现。
- 部署必须同时更新API/web；本次无新迁移、不重导素材。最终远程提交的CI需独立核对；网页目录提交中间状态不用于部署。完整功能/权限与人工检查清单见docs/37。

---

# 2026-09-26 第四批：后台建筑目录分页与读取竞态

- 第三批最终提交`a2ebcfa`的push CI `36231363412`与PR CI `36231365340`成功，真实PostgreSQL及compose-smoke通过；远程文件树与本地验证内容一致。
- 资源后台建筑查询原先固定前50项，只提示输入更完整名称。现使用既有API的page/page_size与分页总数，每页20项，搜索重置第一页，失败可手动重试；候选列表翻页不清空资料编辑区，选另一建筑保留原未保存检查。
- 后台通用读取hook补成功响应取消保护，防止旧分页/建筑请求在新请求后返回并覆盖列表；path变空时结束加载。没有增加自动写入重试，没有修改scope或员工权限。
- 新增3项使用实际hook源码的effect/cleanup回归：旧结果晚于新结果、清空目标后的加载恢复、卸载后的错误/状态隔离；完整前端71项、TypeScript与Vite构建通过。测试使用受控读取与hook适配器，不证明React浏览器布局或真机交互通过。
- 本轮仅前端与文档修改；第三批后端测试基线保持169项，第四批最终CI仍须独立核对。生产未部署，超过50项的真实后台目录、保存草稿时浏览候选列表、手机窄屏分页待部署方验收。

---

# 2026-09-26 第三批：楼层原图恢复与资源验收报告

- 基线`c60ce15`的第二批push CI `36230278400`与PR CI `36230280196`成功：137项后端、61项前端及契约/构建/compose-smoke通过。第三批需要核对自己的最终提交，不能沿用这些运行。
- 原图加载独立设置30秒超时，成功/失败/超时清理定时器和事件监听；切换/卸载后旧load/error不更新当前图片。图片重试只替换Leaflet图层，地图实例与视图位置保持；失败图层移除，不宣称强制终止浏览器底层图片网络传输。
- 新增7项图片生命周期测试：同步缓存命中、网络错误、超时、已加载/加载中卸载、旧事件与新尝试竞争、同步异常。完整68项前端测试及TypeScript/Vite生产构建通过。测试使用事件适配器和控制时钟，不能当作浏览器视觉/触控通过。
- 既有`verify_floor_release.py`新增完整只读JSON报告：输入清单摘要、预期/通过数量、每栋/层/分区状态；单项失败继续检查其他项，区分字段/HTTP/原图字节一致。版本变化不自动降级，额外楼层不混入本批计数；拒绝图片URL错绑及重定向，限制响应大小与检查预算。时间预算在分块读取间检查，可能延长至一次网络超时；不是精确进程终止机制。
- 新增32项脚本回归，覆盖缺层/分区异常/元数据漂移/字节变化、新revision保护、错误页面、访问失败、输出报告和退出码、清单不被报告覆盖、大小/时间限制、20栋96层100图清单兼容。本地HTTP服务执行真实CLI流程；真实FastAPI测试实例验证发布图的原始字节和撤回后的不可读。
- 完整后端165项通过，4项PostgreSQL因本地未配置实例跳过，由最终GitHub CI验证；Ruff通过。独立脚本通过Python 3.8语法检查，实际执行环境为3.12，不冒称在系统3.8实测。
- 没有修改产品API、DTO、依赖、数据库、原始素材、地图几何或平台SDK，没有连接/部署生产服务器。真实站点20栋全量报告、电脑/手机弱网和旋转屏幕验收仍由部署方按docs/17与36执行。

---

# 2026-09-26 第二批：公共资料读取超时

- 第一批最终提交`d49a7e8`的push CI `36230038251`与PR CI `36230041409`均成功：137项后端测试、52项前端测试、契约/构建、PostgreSQL和compose-smoke通过。这是第一批的远程结果，不证明下面新增补丁的CI已通过。
- 阅读下一任务代码确认楼层/全景列表请求无独立超时，轮询取消的异常又被面板忽略，挂起传输可能无限显示加载。第二批在真实公共API客户端增加15秒请求deadline，覆盖headers和JSON解析，超时转为可重试ApiError(408)。不取消面板所属signal；主动取消仍为AbortError，不渲染上一地点错误。
- 新增9项回归（其中3项执行实际API客户端源码），完整61项前端测试与TypeScript/Vite构建通过。覆盖永不完成的传输、响应体挂起、开始前取消、进行中取消、迟到结果、定时器/监听器清理、同步异常、超时/abort竞争及原错误信封保留。
- 无后端、契约、依赖、迁移或素材变动；没有访问生产服务器。逻辑测试不等于真实手机网络/图片/外部VR页面验收，原图加载和真机体验继续按docs/36核查。最终GitHub CI须按此补丁提交单独验证。

---

# 2026-09-26 非智能体持续开发：收藏、检索与浏览历史

- 从远程`5d085aa`增量开发，按用户新指令暂停智能体工作。新增地点收藏、最近20个浏览地点、按校区保存和清空操作；仅存UUID，不上传浏览记录。所有列表从当前公开目录解析，已下架地点不展示。
- 搜索支持全角字符/大小写/空格分词，名称与别名精确匹配优先；目录拆成独立组件，增加加载失败重试、三个范围及空态。
- 用户选点与打开楼层进入浏览历史，后台纠正只replace；Back/Forward恢复同建筑楼层/分区，关闭直接分享的大图仍留在本站。增加楼层/分区链接复制。
- 新增11项前端逻辑回归，合计52通过；TypeScript与Vite生产构建通过。覆盖持久化损坏、容量、拒绝保存、跨标签页已完成修改、ID/公开状态过滤、搜索排序、历史前进/后退及直接链接关闭。
- 未改变后端、DTO、迁移、平台SDK、依赖版本、原图或地图几何；未连接/部署生产服务器。当前浏览器预览此前被策略限制，手机/桌面实际视觉验收待部署方按docs/36执行，不以逻辑测试代替。
- 每小时自动化任务已创建，但未来每次实现/写入仍以实际结果为准。GitHub最后完整提交与CI须独立验证，不把本地测试或历史SHA当作新CI通过。

---

# 2026-09-26 Full SDK一致性修复

- 基线为远程 `0b25f5e`。其CI `36221174235` 的后端135项测试通过，但 `export_contract.py --check` 报OpenAPI漂移；compose-smoke通过。该提交不是可直接沿用的全绿版本。
- 按用户再次提供的Full SDK代码，将后端公开配置、前端加载地址与部署自检统一为 `embedFull.js`，只构造 `HiagentWebSDK.WebClient`。不再隐式选择Lite，也不从嵌入代码推测服务端API。
- 新增3项前端回归：同时存在Full/Lite时使用Full且保留四字段、Lite-only明确不兼容、旧Lite配置在加载外部脚本之前拒绝。原初始化测试增加独立Full URL断言。前端完整41项通过，TypeScript/Vite生产构建通过。
- 新增2项后端回归：真实配置与生成契约必须为Full，部署自检拒绝旧Lite配置且报告不泄露凭据。本地完整133项通过，4项真实PostgreSQL因未配置实例跳过；不把它们记为本地通过。
- 已重新生成完整OpenAPI、前端类型、接口参考和六工具插件文件；原已提交生成物本来就是Full，所以本次重新生成没有产生这些文件的差异。源定义与生成结果一致，而非手改生成物掩盖漂移。
- 从基线对比，地图/楼层资料、83点目录及几何无改动，嵌入页worker CSP保留。无数据库迁移、无种子导入、无生产连接或部署。
- 用户报告学校API说明页失效，新增docs/35记录替代官方证据、访问排查和授权门槛。没有调用所提供的API密钥，不复用Cookie/JWT，不把账号AccessKey用于公众代理；`capabilities.chat`仍false。
- 该记录是本地修复验证结果。GitHub原生网页上传将产生不同提交ID；最新完整提交的两项CI应按其实际运行结果验收，不沿用上方基线或历史绿勾。真实学校问答、访客登录、引用与手机界面仍待部署方验证。

---

# 2026-09-25 GitHub网页交付

- 用户明确授权使用其GitHub登录会话提交代码。GitHub连接器写入403后，改用GitHub原生文件上传页面，在 `feat/admin-console` 上按目录提交本轮完整实现与配套资料，不修改集成权限、不合并main、不部署服务器。
- 网页提交会产生新的Git提交ID，与离线包内三个本地提交ID不同；应核对最终文件树内容，不能仅比较HEAD字符串。已拉取本轮远程代码时不要再导入离线包。
- 中间目录提交使用 `[skip ci]`，全部文件就绪后由最终完整提交运行 `contracts-and-tests` 与 `compose-smoke`。检查GitHub对应提交的实际状态，不把下方历史本地测试或其他SHA的绿勾当作此次远程CI通过。
- 下面两节保留首次离线交付时的验证结果和限制；学校真实聊天、访客登录、引用、变量消费及手机视觉仍需部署后验收。

---

# 2026-09-25 续交付：部署只读自检（离线交付时记录）

- 新增Python3.8+标准库命令 `scripts/check_nk_genios.py`：只发GET，检查公开配置、启用状态、主页面与嵌入页、构建脚本、每条CSP、六个公开插件接口与导览链接目标。可选检查学校SDK的HTTP响应，始终把真实聊天/登录/引用/变量与手机验收列为未执行。
- 报告不保存appKey、服务端密钥、响应原文或错误跳转地址；拒绝带凭据的目标和跳转，不覆盖已有报告文件。明确区分PASS/FAIL/WARN/SKIP；没有已发布点位时不能声称四个点位工具通过。
- 新增15项测试通过，使用本机HTTP服务连接真实FastAPI测试路由与明确的HTML/CSP测试夹具，覆盖错误回退页、多条CSP、错误链接目标、重定向、凭据不回显、空资料及响应大小限制。该测试不是实际Nginx或学校SDK验收。
- 后端完整回归131通过、4项真实PostgreSQL仍跳过；Ruff及Python3.8语法检查通过。前端源代码未变，沿用同一前端代码的38项测试与生产构建结果。
- 将自检加入compose-smoke；离线制包时远程写入权限受阻，包内提交CI未运行。本地安装数据库/Nginx依赖被执行环境的系统权限限制阻止，未绕过限制，也未把容器或生产环境记为通过。
- 新包保留前版提交并增加自检提交，支持从基线或已导入前版的仓库快进。服务器仍由用户/DeepSeek部署，未连接或修改生产环境。

---

# 2026-09-25 小开WebSDK与平台配套（首次离线交付时记录）

- 使用用户提供的 `embedFull.js` / `WebClient` 四字段构造协议，实现独立网页容器、桌面/手机布局、按需加载、收起保留、手动重载、超时/错误降级、地点和楼层提问入口、可选十字段公开上下文。布局已有代码与构建验证，未完成真实浏览器视觉验收。
- 新增公开嵌入配置与导览聚合两个接口，完整契约82操作（46已实现、36planned）；新增chat_embed能力，后端chat仍关闭。复用现有公开状态与资源过滤，无数据库迁移、无素材改写、无生产内容导入。
- 后端本地116通过，4项真实PostgreSQL因未配置实例跳过。覆盖私有/草稿/下架/停用校园过滤、楼层版本匹配、模块开关、公开appKey与服务端密钥分离、可信链接origin、中文别名与字面量搜索、只读插件裁剪、知识导出成功/失败/重复目录。新增PostgreSQL别名查询门槛纳入CI。
- 前端38项Node测试通过，其中新增SDK真实初始化源码在受控JS测试环境下的单次初始化、错误来源消息拒绝、空变量开关、脚本失败/不兼容/构造异常/超时与迟到加载；公开上下文白名单、消息来源/版本校验与VR分享状态。测试未调用真实平台、未生成模拟AI答案，不作为学校实聊成功证明。
- TypeScript与Vite生产构建通过；包含index.html及agent/embed.html两个构建入口。Ruff、完整OpenAPI/前端类型/接口参考、插件文件与100题生成一致性检查通过。CLI脚本按Python3.8语法解析通过，实际本地运行Python3.12。
- 生成两个只读插件文件：OpenAPI3.0.3为29,045字节，3.1为28,670字节；均只含六个公开GET操作且引用完整。平台导入兼容性仍需实际确认。
- 100题为未执行草案，参考材料为空、结果not_run；没有编造正确率。提示词、变量表、知识库维护/导出、平台与部署操作见32/33。
- 执行环境访问学校SDK返回502；云浏览器访问本地预览返回ERR_BLOCKED_BY_CLIENT。没有绕过访问限制；未完成学校登录/访客身份/真实问答/引用/变量消费/链接目标与手机软键盘验收。
- 本地无Docker、Nginx与PostgreSQL可执行环境，容器CSP/生产数据库测试需CI及部署方执行；不沿用旧SHA结果。首次交付时普通Git推送无凭据，已连接GitHub创建tree返回403 Resource not accessible by integration，当时代码尚未推送、远程CI未运行，使用Git bundle和补丁备用交付。后续已按用户授权改用GitHub原生网页提交，见本文件顶部；34仅保留离线备用步骤。
- 未连接/部署用户服务器；仍由用户与DeepSeek按32同时更新API和web。旧地图图块、楼层原图、坐标与审核发布流程不变。

---

# 2026-09-24 界面精简与完整冲刺规范

- 实现地图主区域、按需搜索/目录、单点详情、手机可展开底卡、真实已发布资源入口和单次进入楼层大图；移除欢迎横幅、常驻目录、重复楼层页签/小预览及无VR空区块。地图图块、标注、正式坐标、原图和后台发布流程未修改。
- 楼层资源预读不自动弹窗、不自动生成floor参数；合法深链接首次可开对应层/区；关闭后轮询不重开；跨建筑链接、撤回楼层/分区只回退合法标注资源。网络错误与空数据区分。
- 本地前端30项Node测试通过，含新增5项上述楼层状态回归；TypeScript与Vite生产构建通过。现有坐标/公开同步/分享/介绍来源测试保留。Ruff、OpenAPI同步、完整接口参考生成一致性检查通过。
- 新增docs/21—31：完整产品需求、界面、架构与数据、智能体、资源/VR、主题/路线、运营/渠道、质量评测、逐项冲刺任务、本轮部署及自动生成的接口/字段参考。31覆盖80项操作，其中44已实现、36planned；所有未来能力明确标待开发。新增CI检查防止接口参考漂移。
- 规范统一旧动作别称为contracts.py枚举；virtual主题与M03步行路网解耦；旧计划角色不冒充现有四角色；机器契约未改变。
- 云浏览器拒绝本地HTTP/文件预览，因此没有完成本轮电脑/手机实际视觉、触控、系统键盘或读屏验收，也不以截图/模拟数据冒充通过。部署方必须按docs/30执行真机清单；未尝试绕过浏览器访问策略。
- 本轮不连接/部署用户服务器、不导入生产资料、不调用NK-GeniOS；没有API/迁移/素材变化。GitHub CI按最终提交单独核验，不沿用上一SHA的结果。

---

# 2026-09-24 83点介绍与来源展示

- 新增83条介绍初稿、35个学校官方来源和1个项目组地图来源；53条有官方资料支持，30条仅提供现有地图可确认的信息。完整审阅稿由同一JSON生成，覆盖83个既有UUID；不是宣传部门已审核或服务器已发布证明。
- 内容包不携带坐标、点击轮廓、别名、分类或底图。标准库脚本默认只预览，经显式参数可保存/提交现有后台草稿，不能发布；使用现有权限、scope、会话、CSRF和双revision检查。
- 本地HTTP集成验证：在用户已修正的正式点位上补介绍并独立审核，前后几何（包括入口ID、label_on_map）、名称、别名、分类完全相同；发布前公开介绍不变，发布后公开接口读取新正文。自审、viewer写入、超scope拒绝；已有正文/草稿保留；并发地图修改409；提交响应丢失不重试。
- 本地后端97通过、3项真实PostgreSQL测试因本地无实例跳过；Ruff及OpenAPI同步检查通过。标准库脚本通过Python 3.8语法检查；未在系统Python 3.8上执行真实服务器登录。
- 前端25项测试通过，TypeScript与生产构建通过。新增全部83条正文/来源解析、旧正文兼容、非法URL/日期退回普通文本检查。访客正文提高字号和对比度、分段显示，来源默认折叠；没有重写整个前台布局。
- 本次不改变API、数据库迁移、校园图块或楼层原图。没有连接服务器、导入生产介绍或替用户审核。整体UI精简、真实AI、主题参观仍为docs/20中的计划。
- 新布局的部署后桌面/手机视觉核验、第三方来源长期可用性与宣传口径审核仍须执行；构建/自动测试不冒充这些验收。GitHub CI结果按实际提交单独核对。

---

# 2026-09-24 完整楼层原图与后台楼层/VR编辑

- 20栋建筑、96层、100张标注原图逐张核对；第20栋综合实验楼C区已纳入。体育馆1、2层各A/B/C，3、4层各一张；图书馆没有6层，不补造。
- 完整ZIP含manifest+100文件，81,728,317字节；SHA256 `fee22db8d4ab112da8eeb524c833d631dcb5d3a4236e613e72dafb99c63725d2`。打包读取逐图摘要匹配，无照片或无标注图。
- 临时SQLite演练0004→0005迁移、alembic check、历史图书馆1层revision2→96层全量导入→重复导入；原有83点及全部几何逐字段不变，包含模拟已审核后台编辑。图书馆1层旧清单摘要不变。
- 实际HTTP TestClient逐栋/层/分区读回100图，原尺寸/字节数/MIME/SHA256全部匹配；严格验收脚本能发现缺失楼层。机器记录见data/floors/jinnan-v1/verification-20260924.json。
- 本地后端81项通过，3项真实PostgreSQL测试因无本地实例跳过。覆盖原图上传、作用域/CSRF、禁止上传者与贡献者自审、草稿隔离、替换/下架/恢复、旧版本/跨建筑拒绝、非法图片/VR链接、VR独立开关和退回重提。
- 前端TypeScript与生产构建通过，22项导航/几何/公开同步测试通过；Ruff（API显式配置）与自动生成契约同步检查均通过。新增后台界面未完成云浏览器桌面/手机视觉验收：此前本地预览被策略阻止，未通过其他浏览器通道绕过。
- PostgreSQL17迁移及点位/楼层审核发布回归已纳入CI；最终提交的contracts-and-tests和compose-smoke结果需按GitHub该提交查看，不沿用旧提交的状态。
- 新后台为楼层标注原图和外部HTTPS VR链接维护；不代表完整媒体CMS、房间导航、AI或VR托管已实现。原图内容人工核对不等于逐房号和现场用途核验。
- 本次未连接/部署用户服务器。GitHub提交、Release文件上传和服务器导入属于不同环节；用户/DeepSeek按docs/17-floor-release.md执行部署验收。

---

# 2026-09-23 审核后客户端更新修复

- 只读访问2512921.cn：公开状态返回v0.4.0、admin=true；首页加载index-CQXYMQJx.js，对应修复前构建。地图revision=3，公开83点当时均为point.revision=1；anchor/polygon逐项与初始资源清单一致。此证据不能确定用户具体一次审核的状态，仍需对应点位名称/ID与后台完整地址核对。
- 源码确认两个问题：公开App仅初次读取点位；后台点击定位/拖动锚点只改anchor，而公共点击与定位使用polygon。已分别增加刷新生命周期和整体平移。
- 前端20项测试通过，包含原8项与新增12项：移动范围、越界拒绝、发布发生于旧请求期间、断网/超时与重试、卸载取消、可见页面轮询、focus/online/广播、广播不可用降级、数据引用保持、下架选择清除、完整分页/图版本、发布回读与不一致提示。TypeScript及生产构建通过。
- 未更改后端业务、API DTO、数据库迁移或图片字节。正式点位revision与底图revision仍分别管理。
- 未登录用户后台核对具体审批记录，未SSH或部署用户服务器；真实桌面/手机UI验收待部署。自动化单元测试不冒充浏览器交互验收。
- GitHub当前提交的CI结果在交付时另行核对，不沿用上一提交的通过状态。部署与验收见docs/16-publication-sync-fix.md。

---

# v0.4.0 管理后台验证

本轮在`feat/admin-console`上新增管理后台，部署说明见docs/15-admin-console.md。本记录不表示用户服务器已经升级。

- 本地后端新增12项（含参数化）权限与工作流测试；与地图、楼层回归合计57通过。真实PostgreSQL迁移与后台工作流2项因本地未配置实例跳过，交由当前提交GitHub CI执行。
- 已验证：未登录拒绝、功能开关、Cookie摘要保存、Origin与CSRF、禁止自审、权限scope与越权访问、草稿不影响公开版本、退回重提、发布、下架、恢复、旧revision409、非法边界、强制改密、停用会话撤销、登录限流、过期/退出、密码不出现在审计/用户响应。
- SQLite `alembic upgrade head`、`alembic check`通过；新增head `0004_admin_console`。正式部署须通过当前提交PostgreSQL17测试与Compose冒烟。
- 前端生产构建、TypeScript与8项导航/几何测试通过。几何测试覆盖任意方向框选、原生像素边界、凹多边形、自交/重复/退化/非法值拒绝；不是浏览器交互测试。
- 原校园图块及楼层素材没有变更。公开文字层由数据库`label_on_map`控制；迁移与旧包兼容保留雕像文字。
- OpenAPI及前端类型从实际DTO生成；已实现点位后台接口已移出planned路由，未实现素材/SSO等仍标planned。
- 会话失效后的保留输入、手机触摸和完整视觉检查仍需部署后的真实浏览器验收。此前本地预览被云浏览器策略阻止，不通过其他浏览器表面绕过。
- 未连接或部署用户服务器；GitHub只交付实现与部署说明。

---

以下为原版本历史记录，其数量与功能状态仅对应当时提交。

# v0.3.0 / M05a 楼层查看与导入验证记录

本记录对应楼层查看、图片导入和校园点击区域扩展。M00/M01历史证据见历史提交；本记录不代表用户服务器已更新或校方已经审核发布。

## 已执行

| 检查 | 结果 |
| --- | --- |
| Ruff、OpenAPI同步 | 通过；楼层列表/详情改为已实现，增加原图读取接口 |
| 后端测试 | 39通过，1项真实PostgreSQL测试因本地无实例跳过 |
| 楼层数据与图片 | 两种图片原始字节、尺寸、MIME、SHA256、排序及楼层/地图一致性通过 |
| 访问控制 | 草稿、停用校园、隐藏建筑/地图、关闭楼层能力、旧版本、非法图片类型、越界路径均拒绝直读 |
| 资源导入 | 双图配对、整批校验、损坏文件、额外实拍文件、不可变版本、ID/楼层序号冲突与审核记录通过 |
| SQLite迁移 | upgrade head和alembic check通过，head为0003_floor_plans |
| TypeScript与生产构建 | 通过 |
| 楼层分享回归 | 5项通过：同建筑重选、切换/关闭建筑、迟到响应、无效/跨建筑楼层、空楼层/返回概览 |
| 本地HTTP冒烟 | Vite代理下健康检查、校园/点位、前端入口、版本2地图与PNG图块通过；图书馆1层实际发布双图的HTTP读取与SHA256一致性通过 |
| 校园地图版本2 | 23个点击区域（含19栋楼层资料对应建筑）；新增点击范围为人工对照底图绘制，未经实测 |
| 地图原画质 | 复用原有265块PNG，逐文件SHA256与版本1相同；没有重新编码或降采样 |
| 飞书目录 | 已核对前19项、91层；第20项排除；公共接入清单不含可转入实拍照片目录的源链接 |

## 材料交付边界

- **91层完成目录和稳定ID，其中图书馆1层已取得、配对并在本地导入；其余90层仍待原文件。** 二层原文件下载未成功取得完整文件；没有用预览截图补作原图。
- 未将实拍照片加入仓库、资源包或网页。楼层包必须显式指定clean/labeled两种文件；未知文件会阻止导入。将实拍照片错误指定为clean/labeled仍需人工审核识别，程序不能自动证明图片内容类别。
- 已交付校园地图版本2资源包及图书馆1层双图资源包。该楼层包完整包含1层2图，不能作为91层完整包使用。部署代码后仍需按文档导入资源包；未导入资料的建筑会显示“暂无已发布楼层”。
- 接到整理后的无标注/已标注原文件ZIP后，可直接按docs/11-floor-plans.md生成资源包并审核导入，不需要重写前端。

## 未运行与限制

- 本地没有Docker Engine和真实PostgreSQL。PR #3基线提交31cb612的CI已经通过39项后端测试、PostgreSQL迁移、前端构建和Compose冒烟。本次后续提交的CI结果以该PR最新检查为准；不沿用旧提交证明新提交。
- 云浏览器拒绝访问本地预览地址，本轮没有完成新增楼层界面的桌面/手机实机交互验收。前端构建通过不等于视觉验收通过。
- 本轮GitHub连接器写入返回403；沿用用户授权的GitHub登录网页交付，不改变集成权限。
- 未连接服务器，未变更2512921.cn。部署由用户与DeepSeek完成。
- 房间、室内路线、AI、VR、员工SSO和后台审核界面尚未实现。当前使用有审核记录的服务器CLI导入，源文件发布权限由实际负责人确认。
- 原图展示保留原文件像素和字节；浏览器缩放仅影响显示比例。允许公开展示的图无法防止用户截屏，此功能不承诺杜绝外流。

## 部署方验收

记录实际commit、镜像digest、迁移head、地图/楼层revision、资源包摘要与审核信息；执行health/ready和scripts/smoke.py，再用手机验证地图→建筑→楼层→图种切换、缩放、关闭和直达链接。不要记录密码或未经审核的照片。

## 2026-09-23 最新命名范围与有标注楼层改版

- 校园地图v3：83个已命名点位，未命名区域0个；原23点位及19组楼层绑定UUID不变。按用户新要求，周恩来雕像位于两座业务楼正中央；其余3个撤回文化点位不恢复。
- 地图包86,748,264字节，SHA256 `7f3020a694eb5739638ef5480edc7fe4dd8ef700900c0e32e271129d65ec2ac8`；265张PNG瓦片逐一校验与v2一致。
- 图书馆1层新包981,404字节，SHA256 `3a4988778866beaa831aa6f938ab46dc36ac98262206942e525321fbcca75281`；revision 2、仅labeled.png与manifest。该PNG与已核对的标注原文件逐字节相同。
- 后端Ruff检查通过；pytest本地45通过，1个PostgreSQL测试因无本地实例跳过；OpenAPI同步检查通过。
- 新增单张标注图导入、拒绝仅无标注图、生成器不读取无标注/照片路径、雕像处于两楼中央的回归测试。旧双图数据在公共API中仅返回标注图，clean读取拒绝。
- 新版生产页面和全机型触摸验收尚未进行；此记录不代表已部署服务器。底图含原有烧录文字，清晰名称层不改变其像素。

- 最新前端生产构建/TypeScript检查与10项测试均通过。
- 实际资源在临时SQLite中验证v2地图＋历史双图楼层→83点地图v3＋有标注楼层revision 2的升级；API读取83点，原楼层ID不变，标注图与原尺寸地图图块逐字节一致；旧/新版本clean链接均404。
- 最新smoke脚本在本地HTTP服务上通过：健康/就绪、静态首页、83个校园点位、版本化地图及PNG、图书馆有标注原图SHA；脚本遍历点位分页，不再要求双图。

## 2026-09-23 后续修正：恢复图中文字

- 依据用户最新反馈，移除常驻浮动名称与相关避让算法、样式和名称开关；原规划图中烧录名称正常保留，数字序号未恢复。
- 周恩来雕像保留既有点位和锚点，以原图像素坐标中的普通SVG文字补充，随底图缩放；不重采样/重编码地图。83点目录、地图revision 3、楼层revision 2和接口未修改。
- 本次前端生产构建、TypeScript检查通过；保留的5项楼层导航回归测试全部通过。已删除的5项浮动标签测试只覆盖被撤销的算法，不再计入当前测试数量。
- 新增文字层依次检查点位存在、map_id及revision一致，禁止跨图/跨版本显示；文字层不拦截地图拖动和地点点击。
- 本地组件预览已经构建，但云浏览器安全策略不允许打开本地文件URL，未完成本次浏览器视觉/触摸验收。用户服务器未由本轮操作部署；最终效果需按docs/14-restore-map-lettering.md在部署后验收。
